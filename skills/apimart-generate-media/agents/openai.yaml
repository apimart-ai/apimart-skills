interface:
  display_name: "APIMart Image & Video"
  short_description: "Upload reference images and generate APIMart media"
  default_prompt: "Use $apimart-generate-media to read the live model documentation, upload any local reference image, and safely generate the requested image or video using URL-only audio/video inputs."
